name: schemas

on:
  push:
    branches: [main]
    paths:
      - 'schemas/**'
      - 'examples/**'
      - 'scripts/check_mcp_trust_schemas.sh'
      - 'scripts/verify_examples.js'
      - '.github/workflows/schemas.yml'
  pull_request:
    paths:
      - 'schemas/**'
      - 'examples/**'
      - 'scripts/check_mcp_trust_schemas.sh'
      - 'scripts/verify_examples.js'
      - '.github/workflows/schemas.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Validate schemas and examples
        run: ./scripts/check_mcp_trust_schemas.sh
      - name: Verify example chain consistency
        run: node scripts/verify_examples.js --example
